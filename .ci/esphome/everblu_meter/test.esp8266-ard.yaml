substitutions:
  spi_clk_pin: GPIO14
  spi_miso_pin: GPIO12
  spi_mosi_pin: GPIO13
  gdo0_pin: "4"
  meter_type: water

esphome:
  name: ci-esp8266-ard
  min_version: 2024.6.0

esp8266:
  board: d1_mini
  framework:
    version: recommended

logger:

api:

ota:
  - platform: esphome

wifi:
  ssid: CI-WIFI
  password: CI-PASSWORD

<<: !include common.yaml
